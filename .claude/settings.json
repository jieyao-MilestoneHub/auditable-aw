{
  "includeCoAuthoredBy": true,
  "env": {
    "INSIDE_CLAUDE_CODE": "1",
    "AAW_WORKSPACE": "1",
    "BASH_DEFAULT_TIMEOUT_MS": "420000",
    "BASH_MAX_TIMEOUT_MS": "420000"
  },
  "permissions": {
    "allow": [
      "mcp__aawctl__*",
      "Read",
      "Glob",
      "Grep"
    ],
    "ask": [
      "Bash(npm run:*)",
      "Bash(yarn:*)",
      "Bash(pnpm:*)",
      "Bash(git:*)",
      "Edit"
    ],
    "deny": [
      "Bash(rm -rf:*)",
      "Bash(sudo:*)",
      "Bash(chmod 777:*)",
      "Bash(curl:*)",
      "Bash(wget:*)"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/skill-eval.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "# Prevent editing on main branch\n[ \"$(git branch --show-current 2>/dev/null)\" != \"main\" ] && [ \"$(git branch --show-current 2>/dev/null)\" != \"master\" ] || { echo '{\"block\": true, \"message\": \"[AAW] Cannot edit files on main/master branch. Create a feature branch first.\"}' >&2; exit 2; }",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "# AAW Audit: Log all Bash commands\nmkdir -p /workspace/.aaw/logs 2>/dev/null || mkdir -p \"$CLAUDE_PROJECT_DIR/.aaw/logs\" 2>/dev/null\nLOG_DIR=\"${CLAUDE_PROJECT_DIR:-/workspace}/.aaw/logs\"\necho \"[AAW $(date -Iseconds)] PreToolUse Bash: $TOOL_INPUT\" >> \"$LOG_DIR/hooks.log\" 2>/dev/null || true",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "# Auto-format JS/TS files with Prettier\nif [[ \"$CLAUDE_TOOL_INPUT_FILE_PATH\" =~ \\.(js|jsx|ts|tsx)$ ]]; then\n  file_path=\"${CLAUDE_TOOL_INPUT_FILE_PATH}\"\n  if command -v npx &>/dev/null && [ -f \"package.json\" ]; then\n    npx prettier --write \"$file_path\" 2>&1\n    exit_code=$?\n    if [ $exit_code -ne 0 ]; then\n      echo '{\"feedback\": \"Formatting failed. Check file for syntax errors.\"}' >&2\n      exit 1\n    else\n      echo '{\"feedback\": \"Formatting applied.\", \"suppressOutput\": true}'\n    fi\n  fi\nfi",
            "timeout": 30
          }
        ]
      },
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "# Auto-install dependencies when package.json changes\nif [[ \"$CLAUDE_TOOL_INPUT_FILE_PATH\" =~ package\\.json$ ]]; then\n  echo '{\"feedback\": \"[AAW] Installing dependencies...\"}' >&2\n  npm install >/dev/null 2>&1 && echo '{\"feedback\": \"Dependencies installed.\", \"suppressOutput\": true}' || {\n    echo '{\"feedback\": \"Failed to install dependencies.\"}' >&2\n    exit 1\n  }\nfi",
            "timeout": 60
          }
        ]
      },
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "# Auto-run tests when test files change\nif [[ \"$CLAUDE_TOOL_INPUT_FILE_PATH\" =~ \\.test\\.(js|jsx|ts|tsx)$ ]]; then\n  echo '{\"feedback\": \"[AAW] Running related tests...\"}' >&2\n  if [ -f \"package.json\" ] && grep -q '\"test\"' package.json; then\n    npm test -- --findRelatedTests \"${CLAUDE_TOOL_INPUT_FILE_PATH}\" --passWithNoTests 2>&1 | tail -30\n    exit_code=${PIPESTATUS[0]}\n    if [ $exit_code -eq 0 ]; then\n      echo '{\"feedback\": \"Tests passed.\"}'\n    else\n      echo '{\"feedback\": \"Tests failed. See output above.\"}' >&2\n    fi\n  fi\nfi",
            "timeout": 90
          }
        ]
      },
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "# Type-check TypeScript files\nif [[ \"$CLAUDE_TOOL_INPUT_FILE_PATH\" =~ \\.(ts|tsx)$ ]]; then\n  if [ -f \"tsconfig.json\" ] && command -v npx &>/dev/null; then\n    echo '{\"feedback\": \"[AAW] Checking TypeScript types...\"}' >&2\n    output=$(npx tsc --noEmit 2>&1)\n    exit_code=$?\n    if [ $exit_code -eq 0 ]; then\n      echo '{\"feedback\": \"No TypeScript errors.\", \"suppressOutput\": true}'\n    else\n      errors=$(echo \"$output\" | grep -A 2 \"error TS\" | head -30)\n      if [ -n \"$errors\" ]; then\n        echo '{\"feedback\": \"TypeScript found type errors:\"}' >&2\n        echo \"$errors\" >&2\n      fi\n    fi\n  fi\n  # Non-blocking\n  exit 0\nfi",
            "timeout": 30
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "# AAW Audit: Log session end\nLOG_DIR=\"${CLAUDE_PROJECT_DIR:-/workspace}/.aaw/logs\"\necho \"[AAW $(date -Iseconds)] Session ended\" >> \"$LOG_DIR/hooks.log\" 2>/dev/null || true",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "sandbox": {
    "enabled": true,
    "autoAllowBashIfSandboxed": true,
    "allowUnsandboxedCommands": false
  }
}
